/** Mirrors server/src/shared.ts — keep in sync. */
export const EXPECTED_SRAM_BYTES = 512;
export const EXPECTED_SRAM_HEX_LEN = EXPECTED_SRAM_BYTES * 2; // 1024
export const FIRMWARE_HASH_REGEX = /^[a-fA-F0-9]{64}$/;
